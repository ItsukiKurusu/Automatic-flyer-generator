export interface FlyerTemplate {
  imageArea: string
  catchCopy: string
  description: string
  storeInfo: {
    name: string
    address: string
    hours: string
    tel: string
    access: string
  }
  colorTheme: string
}
